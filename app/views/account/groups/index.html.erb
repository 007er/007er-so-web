<h3>驴妹提示：此页内容为你本人创建的和加入的小组。</h3>
<div class="col-md-12">

  <h2 class="text-center"> 我创建的和加入的小组 </h2>

  <table class="table">
    <thead>
      <tr>
        <th> # </th>
        <th> Title </th>
        <th> Description </th>
        <th> Post Count </th>
        <th> Last Update </th>
        <th colspan="2"></th>
      </tr>
    </thead>

    <tbody>
      <% @groups.each do |group| %>
        <tr>
          <td> # </td>
          <td> <%= link_to(group.title, group_path(group)) %> </td>
          <td><%= render_group_description(group) %></td>
          <td> <%= group.posts.count %> </td>
          <td> <%= group.updated_at %> </td>
          
          <td><%= link_to('Delete', account_group_path(group), method: :delete,
                           date: { confirm: "Are you sure?" }, class: "btn btn-default btn-xs") %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
