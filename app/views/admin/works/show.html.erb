<div class="group">
  <div class="group">
    <%= link_to("Edit", edit_admin_work_path(@work), class: "btn btn-primary pull-right")%>

  </div>
  <h2><%= @work.name %></h2>

  <p><%= @work.description %></p>
</div>
