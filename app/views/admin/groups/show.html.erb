<div class="col-md-4 col-md-offset-4">
  <div class="group">
    <%= link_to("Edit", edit_admin_group_path(@group), class: "btn btn-primary pull-right")%>
  </div>
  <h2><%= @group.name %></h2>
  <p><%= @group.title %></p>
  <p><%= @group.description %></p>
</div>
