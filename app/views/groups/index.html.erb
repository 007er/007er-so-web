<h2>小组</h2>

<table class="table table-hover">
  <thead>
    <tr>
      <th>名称</th>
    </tr>
  </thead>
  <tbody>
    <% @groups.each do |group| %>
      <tr>
        <td><%= group.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>