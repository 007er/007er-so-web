<div id="header">
  <a href="/">Web007erSo</a>
  <div class="buttons">
    <ul >
    <% if !current_user %>
      <button class="button"><%= link_to("注册", new_user_registration_path) %> </button>
      <button class="button"><%= link_to("登录", new_user_session_path) %></button>
    <% else %>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
          Hi!, <%= current_user.email %>
          <b class="caret"></b>
        </a>
        <ul class="dropdown-menu">
          <% if current_user.admin? %>
            <li>
              <%= link_to("Admin Panel", admin_categories_path) %>
            </li>
          <% end %>
            <li><%= link_to('New Post', new_post_path)%></li>
            <li><%= link_to('我的个人资料', user_path)%></li>
            <li><%= link_to('修改个人资料', edit_user_path) %></li>
            <li> <%= link_to("退出", destroy_user_session_path, method: :delete) %> </li>
        </ul>
      </li>
    <% end %>
    </ul>
  </div>
</div>
